# Dependencies
node_modules/

# Environment variables (contains secrets - never commit real values)
.env
.env.local

# Logs
*.log

# Local images to upload (don't commit actual images, keep the folder structure)
scripts/images-to-upload/*
!scripts/images-to-upload/.gitkeep
!scripts/images-to-upload/README.md
